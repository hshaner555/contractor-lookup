[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contractor_lib"]

[project]
name = "shared-lib"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28.0",
    "sqlalchemy>=2.0.0",
    "pymysql>=1.1.0",
]
